package com.agentrediska.hardwarestore.domain.usecase

import com.agentrediska.hardwarestore.domain.repository.AllCategoryRepository

class SetAllCategoriesUseCase( private val allCategoryRepository: AllCategoryRepository) {

    fun setAllCategoryRepository() {
      //  allCategoryRepository.setAllCategory()
    }
}